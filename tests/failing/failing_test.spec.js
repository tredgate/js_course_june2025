import { expect } from "chai";

describe("Padající testy", () => {
  it.skip("1 rovná se 2", () => {
    // Testovací kroky testu
    expect(1).to.equal(2); // v expectu je aktuální hodnota, v equal() očekávaná
  });
});
